# Instant-BigGAN-CLIP

*todo: update readme*
```
kubectl create -f instant-biggan-clip-ingress.yaml
kubectl create -f instant-biggan-clip.yaml
kubectl exec -it instant-biggan-clip-76ffcfb48b-wt67t -- bash
(base) jovyan@instant-biggan-clip:~$ conda activate torch-gpu-clip
(torch-gpu-clip) jovyan@instant-biggan-clip:~$ 
```
